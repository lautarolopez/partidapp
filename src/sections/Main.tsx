import React from 'react';

import Layout from '../components/Layout';

import MatchesList from './MatchesList';

const Main: React.FC = () => (
  <Layout>
    <MatchesList />
  </Layout>
);

export default Main;
